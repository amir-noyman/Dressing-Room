{
  "name": "dressing-room",
  "version": "1.0.0",
  "description": "Figma plugin that scans for untokenized values and rebinds matching design token variables",
  "main": "code.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.58.0",
    "typescript": "^4.9.0"
  }
}
